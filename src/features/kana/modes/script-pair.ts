import type { StudyMode } from '../../../core/contracts/study-mode.js';
import type { KanaItem } from '../../../domain/models.js';
import { distractors, shuffle } from '../../study/question-utils.js';
export const kanaScriptPairMode: StudyMode={id:'kana-script-pair',title:'平片假名配對',description:'建立平假名與片假名之間的直接連結。',supports:(item)=>item.kind==='kana',createQuestion(item,context){if(item.kind!=='kana')throw new Error('Kana mode received non-kana item'); const targetScript=item.script==='hiragana'?'katakana':'hiragana'; const pool=context.allItems.filter((candidate): candidate is KanaItem=>candidate.kind==='kana'&&candidate.script===targetScript); const correct=pool.find((candidate)=>candidate.pairId===item.pairId); if(!correct)throw new Error(`Missing kana pair for ${item.id}`); const wrong=distractors(correct,pool,3,(candidate)=>candidate.kana,context.random); const choices=shuffle([correct,...wrong],context.random).map((candidate)=>({label:candidate.kana,value:candidate.kana})); return {type:'choice',prompt:item.kana,subtitle:`選出對應的${targetScript==='hiragana'?'平假名':'片假名'}`,choices,correctValue:correct.kana};},grade(question,answer){if(question.type!=='choice')return {correct:false,rating:'again'}; const correct=answer===question.correctValue; return {correct,rating:correct?'good':'again'};}};
