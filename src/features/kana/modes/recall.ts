import type { StudyMode } from '../../../core/contracts/study-mode.js';
import type { KanaItem } from '../../../domain/models.js';
import { normalizeText } from '../../study/question-utils.js';
export const kanaRecallMode: StudyMode={id:'kana-recall',title:'看讀音打假名',description:'從讀音主動回想假名；比選擇題更嚴格。',supports:(item)=>item.kind==='kana',createQuestion(item,context){if(item.kind!=='kana')throw new Error('Kana mode received non-kana item'); const accepted=context.allItems.filter((candidate): candidate is KanaItem=>candidate.kind==='kana'&&candidate.script===item.script&&candidate.romaji===item.romaji).map((candidate)=>candidate.kana); return {type:'text',prompt:item.romaji,subtitle:item.script==='hiragana'?'請輸入平假名':'請輸入片假名',placeholder:item.script==='hiragana'?'例如：あ':'例如：ア',acceptedAnswers:accepted};},grade(question,answer){if(question.type!=='text')return {correct:false,rating:'again'}; const normalized=normalizeText(answer); const correct=question.acceptedAnswers.some((candidate)=>normalizeText(candidate)===normalized); return {correct,rating:correct?'good':'again'};}};
