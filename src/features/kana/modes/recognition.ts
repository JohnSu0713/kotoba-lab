import type { StudyMode } from '../../../core/contracts/study-mode.js';
import type { KanaItem } from '../../../domain/models.js';
import { distractors, normalizeText, shuffle } from '../../study/question-utils.js';
export const kanaRecognitionMode: StudyMode={id:'kana-recognition',title:'看假名選讀音',description:'看到假名，快速辨認羅馬字讀音。',supports:(item)=>item.kind==='kana',createQuestion(item,context){if(item.kind!=='kana')throw new Error('Kana mode received non-kana item'); const pool=context.allItems.filter((candidate): candidate is KanaItem=>candidate.kind==='kana'&&candidate.script===item.script); const wrong=distractors(item,pool,3,(candidate)=>candidate.romaji,context.random); const choices=shuffle([item,...wrong],context.random).map((candidate)=>({label:candidate.romaji,value:candidate.romaji})); return {type:'choice',prompt:item.kana,subtitle:item.script==='hiragana'?'平假名':'片假名',choices,correctValue:item.romaji};},grade(question,answer){if(question.type!=='choice')return {correct:false,rating:'again'}; const correct=normalizeText(answer)===normalizeText(question.correctValue); return {correct,rating:correct?'good':'again'};}};
