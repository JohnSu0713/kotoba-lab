import type { StudyMode } from '../../../core/contracts/study-mode.js';
import type { KanaItem } from '../../../domain/models.js';
import { distractors, shuffle } from '../../study/question-utils.js';
export const kanaAudioChoiceMode: StudyMode={id:'kana-audio-choice',title:'聽音選假名',description:'不看提示，只靠聲音辨認假名。',supports:(item)=>item.kind==='kana',createQuestion(item,context){if(item.kind!=='kana')throw new Error('Kana mode received non-kana item'); const pool=context.allItems.filter((candidate): candidate is KanaItem=>candidate.kind==='kana'&&candidate.script===item.script); const wrong=distractors(item,pool,3,(candidate)=>candidate.kana,context.random); const choices=shuffle([item,...wrong],context.random).map((candidate)=>({label:candidate.kana,value:candidate.kana})); return {type:'choice',prompt:'🔊',subtitle:'點喇叭重播，選出你聽到的假名',choices,correctValue:item.kana,speakText:item.kana};},grade(question,answer){if(question.type!=='choice')return {correct:false,rating:'again'}; const correct=answer===question.correctValue; return {correct,rating:correct?'good':'again'};}};
